# aniyomi-library-list-to-mal-xml
Python tool to bulk import Aniyomi anime/manga libraries to MyAnimeList. Features: MAL ID resolution via Jikan API, XML chunking for large libraries, duplicate detection, and proper MAL import format. Supports both anime and manga migrations.
